- name: Reading secrets from vault
  hosts: localhost                                # host group name 
  tasks:
    - name: Read KV2 Secret from Hashicorp Vault
      ansible.builtin.debug:
        msg: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=expense-dev/data/backend token={{ token }} url=https://vault.clouding-app.shop:8200  validate_certs=False') }}"
      # vars: 
      #   secrets: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=expense-dev/data/backend token={{ token }} url=http://vault.clouding-app.shop:8200  validate_certs=False') }}"